[project]
name = "deepmemo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.30.0",
    "openai>=1.30.0",
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "watchdog>=3.0",
    "tavily-python>=0.7.0",
]

[dependency-groups]
dev = []
